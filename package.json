{
  "name": "google-sheets-to-appwrite",
  "version": "1.0.0",
  "description": "A tool to transfer data from Google Sheets to Appwrite",
  "main": "index.html",
  "scripts": {
    "build": "# no build step needed"
  },
  "dependencies": {
    "node-appwrite": "^11.1.0",
    "googleapis": "^129.0.0"
  }
}
